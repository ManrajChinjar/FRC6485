package org.usfirst.frc.team6485.robot;

public class TODO_LIST {

  /*
   * TODO Let the bridge be controlled by the right Xbox stick and be limited by the limit switches.
   * - Turn the BridgeMaintainer class into something like StickDriver but with overrides provided
   * by the Spark Controllers and through Java.
   */

  /*
   * TODO Put offloader stick controls to its own command class like StickDriver if multithreading
   * and the drive train watchdog isn't an issue.
   */

  /*
   * TODO Put encoder wires on bridge motor and offloader motor to determine speed
   * and if they're stopped
   */
  
  /*
   * TODO Work on encoders for drive train, bridge and offloader.
   */
}
